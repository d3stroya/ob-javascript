let altura_cm = 167
let altura_m = 1.67
let peso_kg = 53.8
let altura_m_rounded = Math.ceil(altura_m)
let peso_kg_rounded = Math.floor(peso_kg)
let max_value = Number.MAX_VALUE
let suma = max_value + 1
let comparacion = Object.is(Number.MAX_VALUE, Number.MAX_VALUE + 1);

